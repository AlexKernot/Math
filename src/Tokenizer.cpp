#include "Tokenizer.hpp"
#include <ctype.h>

std::queue<Token *> *Tokenizer::process(std::string input) {
    std::queue<Token *> *returnQueue = new std::queue<Token *>();
    for (int i = 0; i < input.length(); ++i) {

    }
}